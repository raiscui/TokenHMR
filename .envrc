# TokenHMR data directory. Change to an external path if /cloud/cloud-ssd1 is full.
export TOKENHMR_DATA_DIR="${PWD}/data"

# Zip download cache directory for fetch_demo_data.sh.
export TOKENHMR_DOWNLOAD_DIR="${TOKENHMR_DATA_DIR}/.downloads"

# 可选: 加载本地私密配置(不提交到 Git)。
# 用途: 覆盖默认路径,或注入 GitHub PAT 等敏感变量(例如 GITHUB_TOKEN)。
if [ -f ".envrc.private" ]; then
  source_env ".envrc.private"
fi
